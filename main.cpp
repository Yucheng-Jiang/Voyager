#include <iostream>
#include <fstream>
#include "voyager.h"
#include <string>
#include <vector>

int main(int argc, const char *argv[]) {

    //TODO: Write your code HERE:
    Voyager* voyager = new Voyager();
    // std::vector<std::string> apts = voyager->getApt();
    // for (auto apt : apts) {
    //     std::cout << apt << std::endl;
    // }
    std::cout << "I love cs225" << std::endl;
    return 0;
}